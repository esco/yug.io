define(function(){

	var Router = Backbone.Router.extend({

		routes: {
			'build': 'index',
			'build/query': 'query'
		},

		index: function(params) {

		},

		query: function(params) {

		}

	});

	return Router;
});
